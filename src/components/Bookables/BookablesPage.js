import BookableList from "./BookablesList";

function BookablePages() {
    return(
        <main className="bookables-page">
            <BookableList />
        </main>
    );
}

export default BookablePages;